#include<stdio.h>//quick

int main(){
	int array[12];
	llenar(array,12);
	quick(array);
}
void llenar(int array,int t){
	int i;
	for(i=0;i<t;i++)
		scanf("%d",array[i]);
}

array quick(int array[]){
	pivote(array);
	
}

int pivote(array){
	
}
